<!DOCTYPE html>
<html>
<head>
  <title>Broncohack</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <script src="http://cdn.jsdelivr.net/emojione/2.1.4/lib/js/emojione.min.js"></script>
<link rel="stylesheet" href="http://cdn.jsdelivr.net/emojione/2.1.4/assets/css/emojione.min.css"/>
</head>
<body>
<%= render 'layouts/navbar' %>
<%= yield %>

</body>
</html>

<script type="text/javascript">

$("#tinderslide > ul li").tsort({attr:"id"});

$('.actions .like, .actions .dislike').click(function(e){
  e.preventDefault();
  $('#tinderslide').jTinder($(this).attr('class'));
});

$("#tinderslide").jTinder({
  onDislike: function(item){
    alert('Dislike image ' + (item.index()+1));
  },
  onLike: function (item) {
    var myId = $("#tinderslide > ul li").last().attr("id");
    $("#user_" + myId)[0].click();
  },
  animationRevertSpeed: 200,
  animationSpeed: 400,
  threshold: 1,
  likeSelector: '.like',
  dislikeSelector: '.dislike'
});


</script>